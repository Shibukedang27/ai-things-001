[pytest]
testpaths = tests
asyncio_mode = strict
markers =
    unit: fast unit tests for schemas, validators, utilities, and providers
    integration: cross-module workflow tests that avoid external infrastructure
    api: REST API contract and middleware tests
    database: SQLAlchemy model and Alembic migration integrity tests
    auth: authentication, authorization, and session-security tests
